console.log("Snake")

class Snake {
    constructor() {
        // should create two snake tiles (head and tail), put them in the snake array.
    }

    // The actual snake
    // SnakeTile[] snakeArray

    // To manage and calculate score, keep a score object
    // Score score

    // Bit isSnakeAlive
}